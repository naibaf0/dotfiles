#!/bin/sh

BEAMER=$1

xrandr --output $BEAMER --primary --auto --output LVDS1 --auto --left-of $BEAMER

~/.fehbg

bspc desktop media -m $BEAMER
bspc desktop browsebig -m $BEAMER

bspc monitor $BEAMER -o media browsebig
bspc monitor $BEAMER -d media browsebig

